# Backend API URL (used by Next.js API routes server-side)
FASTAPI_URL=http://localhost:8000

# Note: This is NOT a NEXT_PUBLIC_ variable - it stays server-side only
# The browser makes requests to Next.js, which proxies to FastAPI
